# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11

# Bayesian inference
pymc>=5.10
arviz>=0.17

# Plotting
matplotlib>=3.8

# Dev / testing
pytest>=8.0

# Optional: X (Twitter) sentiment collector (scripts/08_collect_x.py)
# Install only if you have X API credentials:  pip install tweepy
# tweepy>=4.14

# Optional: LLM judge / feature extractor (provider-agnostic — install the one
# matching your key). Set OPENAI_API_KEY or ANTHROPIC_API_KEY.
# openai>=1.40        # for OPENAI_API_KEY
# anthropic>=0.40     # for ANTHROPIC_API_KEY
